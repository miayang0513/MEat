extends main

block header 

block body
  div(class="login_banner d-flex align-items-center")
    div(class="overlay position-absolute w-100 h-100")
    div(class="col-md-3 m-auto")
      div(class="card card-body")
        h1(class="text-center mb-3") Login
        form(action="/user/login" method="POST" class="needs-validation" novalidate)
          div(class="form-group")
            label(for="email") Email address
            input(type="email" id="email" name="email" placeholder="email" class="form-control" required)
            div(class="invalid-feedback") Please enter a valid email address
          div(class="form-group")
            label(for="password") Password
            input(type="password" id="password" name="password" placeholder="password" class="form-control" required)
            div(class="invalid-feedback") password is required
          button(type="submit" class="btn btn-info btn-block") Login
          a(href="/auth/facebook" class="btn btn-primary  btn-block") Facebook Login
          a(href="/auth/google" class="btn btn-danger btn-block") Google Login
        p(class="lead mt-4")
          | No Account? 
          a(href="/user/register") Register
append script
  script(src="/javascript/inputValidate.js")